{
  "name": "log-storage-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for LogStorage smart contract",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/",
    "create-local": "graph create --node http://localhost:8020/ log-storage-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ log-storage-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 log-storage-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.56.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}